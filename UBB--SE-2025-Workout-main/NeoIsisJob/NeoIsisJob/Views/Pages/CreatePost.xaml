<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DesktopProject.Pages.CreatePost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DesktopProject.Pages"
    xmlns:components="using:DesktopProject.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Top Bar -->
            <RowDefinition Height="*"/>
            <!-- Scrollable Content -->
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <components:TopBar Grid.Row="0" x:Name="TopBar"/>

        <!-- Scrollable Main Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto">
            <Grid Margin="40,20,40,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                </Grid.ColumnDefinitions>

                <!-- Form Container (updated to white background) -->
                <Border Grid.Column="1" Background="#fff" CornerRadius="16" Padding="40,32,40,32" HorizontalAlignment="Center" VerticalAlignment="Top" MaxWidth="700">
                    <StackPanel Spacing="20">
                        <!-- Title Section -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Post Title" Foreground="#2563eb" FontSize="20" FontWeight="SemiBold"/>
                            <TextBox x:Name="TitleInput" PlaceholderText="Enter your post title..." MaxLength="50" FontSize="16" Background="#f3f4f6" Foreground="Black" BorderThickness="0" CornerRadius="8" Margin="0,6,0,0" Padding="12"/>
                            <TextBlock x:Name="TitleCharCounter" Text="0/50" Foreground="#6b7280" HorizontalAlignment="Right"/>
                        </StackPanel>

                        <!-- Content Section -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Description" Foreground="#2563eb" FontSize="20" FontWeight="SemiBold"/>
                            <TextBox x:Name="DescriptionInput" PlaceholderText="Write your post description..." AcceptsReturn="True" TextWrapping="Wrap" MaxLength="250" FontSize="16" Background="#f3f4f6" Foreground="Black" BorderThickness="0" CornerRadius="8" Margin="0,6,0,0" Padding="12" MinHeight="150"/>
                            <TextBlock x:Name="DescriptionCharCounter" Text="0/250" Foreground="#6b7280" HorizontalAlignment="Right"/>
                        </StackPanel>

                        <TextBlock x:Name="Confirmation"/>

                        <!-- Visibility Selection -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Visibility" Foreground="#2563eb" FontSize="20" FontWeight="SemiBold"/>
                            <ComboBox x:Name="VisibilityComboBox" Background="#f3f4f6" Foreground="Black" CornerRadius="8" Margin="0,6,0,0" Padding="12" SelectionChanged="VisibilityComboBox_SelectionChanged">
                                <ComboBoxItem Content="Public" Tag="Public"/>
                                <ComboBoxItem Content="Friends" Tag="Friends"/>
                                <ComboBoxItem Content="Private" Tag="Private"/>
                                <ComboBoxItem Content="Groups" Tag="Groups"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- Group Selection (Visible only for Groups visibility) -->
                        <StackPanel x:Name="GroupSelectionPanel" Spacing="8" Visibility="Collapsed">
                            <TextBlock Text="Select Groups" Foreground="#2563eb" FontSize="20" FontWeight="SemiBold"/>
                            <ListBox x:Name="GroupsListBox" Background="#f9fafb" Foreground="#222" SelectionMode="Single" MaxHeight="200" BorderThickness="0" CornerRadius="8" Margin="0,6,0,0" Padding="12">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <CheckBox Content="{Binding Name}" Margin="5"/>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>

                        <!-- Tags (updated to pill-shaped, colored radio buttons) -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="Tags" Foreground="#2563eb" FontSize="20" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,10,0,0">
                                <RadioButton x:Name="MiscRadioButton" Content="Misc" Background="#e0e7ff" Foreground="#2563eb" BorderThickness="0" CornerRadius="999" Padding="8,18" FontWeight="Medium"/>
                                <RadioButton x:Name="FoodRadioButton" Content="Food" Background="#fef9c3" Foreground="#b45309" BorderThickness="0" CornerRadius="999" Padding="8,18" FontWeight="Medium"/>
                                <RadioButton x:Name="WorkoutRadioButton" Content="Workout" Background="#bbf7d0" Foreground="#15803d" BorderThickness="0" CornerRadius="999" Padding="8,18" FontWeight="Medium"/>
                                <RadioButton x:Name="BlessedRadioButton" Content="Blessed" Background="#fee2e2" Foreground="#b91c1c" BorderThickness="0" CornerRadius="999" Padding="8,18" FontWeight="Medium"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Error Message & Action Buttons -->
                        <StackPanel Spacing="20">
                            <TextBlock x:Name="ErrorMessage" Foreground="Red" Visibility="Collapsed"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                                <Button Content="Cancel" Background="#f3f4f6" Foreground="#222" CornerRadius="8" FontWeight="Medium" Padding="10,24" BorderThickness="0" Click="CancelButton_Click"/>
                                <Button Content="Post" Background="#2563eb" Foreground="White" CornerRadius="8" FontWeight="Medium" Padding="10,24" BorderThickness="0" Click="PostButton_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>
